name: ${project.name}
main: de.JeterLP.ChatManager.ChatEX
version: ${project.version}
author: TheJeterLP
description: Chat management plugin
softdepend: [PermissionsEx,Vault,Factions,mcore]

commands:
    chatmanager:
        usage: /<command> reload|clear
        description: Reloads ${project.name}
        aliases: [chat]
